#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b, c;
    cin>>a;//enter all a, b, c separated by spaces and see what happens
    if(a>0)
    cin>>b;//since b, c are already in input stream, user is not prompted for input again
    cin>>c;
    cout<<a<<b<<c;

}