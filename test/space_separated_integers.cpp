#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t,input;
    cin>>t;
    while(t--)
    {
        cin>>input;//only reads inntegers, not the spaces, so enter one time only
        cout<<input;
    }
}